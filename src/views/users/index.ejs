<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List User</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container position-absolute top-50 start-50 translate-middle">
        <div class="header">
            <h1>User List</h1>
        </div>
        <a href="/create-user" class="badge bg-success">Create new</a>
        <table class="table-responsive table table-centered mb-0">
            <thead>
                <th>ID</th>
                <td>First Name</td>
                <td>Last Name</td>
                <td>Email</td>
                <td>Action</td>
            </thead>

            <body>
                <% for(var i=0; i < userData.length; i++) { %>
                    <tr>
                        <td>
                            <%= userData[i].id %>
                        </td>
                        <td>
                            <%= userData[i].firstName %>
                        </td>
                        <td>
                            <%= userData[i].lastName %>
                        </td>
                        <td>
                            <%= userData[i].email %>
                        </td>
                        <td>
                            <a href="/edit-user?id=<%= userData[i].id %>" class="badge bg-warning text-dark">Edit</a>
                            <a href="/delete-user?id=<%= userData[i].id %>" class="badge bg-danger">Delete</a>
                        </td>
                    </tr>
                    <% } %>
            </body>
        </table>
    </div>
</body>

</html>